{
  "name": "dsh-plugins-plus",
  "private": true,
  "type": "module",
  "description": "SparkElf-maintained independent plugins for DeepSeek Harness (dsh), installable through profile bundles and reusable published capability packages.",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "build": "pnpm --filter @sparkelf/dsh-mobile-bridge build && pnpm --filter @sparkelf/dsh-dataops-integration build && pnpm --filter @sparkelf/dsh-dataops-managed build && pnpm --filter @sparkelf/dsh-chart build && pnpm --filter @sparkelf/dsh-query-result-analysis build && pnpm --filter @sparkelf/dsh-univer-government-docs build",
    "test": "vitest run packages",
    "test:system": "playwright test --config=playwright.config.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/nodemailer": "^8.0.1",
    "@types/ws": "^8.5.0",
    "jsqr": "^1.4.0",
    "playwright": "^1.61.1",
    "pngjs": "^7.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.8"
  }
}
